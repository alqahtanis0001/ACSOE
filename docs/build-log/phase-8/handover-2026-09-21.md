# Phase 8 handover — 2026-09-21, the overnight session

**Read this first if you are picking Phase 8 up from files alone.** It is the single handover
file for the phase; update it in place, do not start a second one. Authority for what Phase 8 is:
the operator's rulings, recorded in `decisions-2026-09-21.md`; the costing and the findings are
`docs/dataset/phase-8-findings.md`; the task list is `feature-specs/PHASE-8-TASKS.md`.

## CURRENT STATE

**The scope the operator ruled** is the 5–8 hour version: F1, F3, a smoke run, and access-control
items 1 and 2. F2 is costed (3.5–5.5 h) and **not started** — the operator's condition is items
1 to 5 done and green first. Paper mode against the **real** exchange; `mode: live` is not to be
enabled.

| Item | State | Evidence |
|---|---|---|
| 1. F1 — the client forwards `recent_trades`/`drain_gaps` | **done, pushed** | `829e4cc`, gate `p8-f1-facade` exit 0; criterion `live_client_serves_the_stream` PASS |
| 2. Read-only key for `fees.py` — everything but the key | **done, pushed** | `cedddaf`, gate `p8-fees-readonly` exit 0; instructions in `readonly-key-instructions.md`. **The key itself is the operator's to create.** |
| 3. F3 — pair rules and balances keyed by engine names | **BUILT AND PROVEN, NOT PUSHED — no gate obtainable** | `p8-f3b` red on my own defect (D13), fixed in one attempt. Then **six consecutive pytest crashes** across `p8-f3b`/`f3c`/`f3d`/`f3e` and two worktrees, eight distinct sites, two of them in pure Python. Proof of the content: `pair_rules_key_on_engine_names` PASS in **four** gate runs on blob `2779ca6166dcdf52`, one full suite at 3,991 passed whose only non-pass was the known pyyaml fault, mypy and ruff clean. **Not a green gate and not called one.** Pushed to branch **`phase-8-f3-ungated`** at `ce54fa6` (commit subject says DO NOT MERGE WITHOUT A GREEN GATE), and restored into `main`'s working tree unstaged, blob `2779ca6166dcdf52`, so the morning's gate has files to copy |
| 4. Smoke run — the daemon against real Kraken, paper mode | **ran; blocked on a ruling** | run `run-20260921T044200717376`, 22 ticks, 668 pairs subscribed. **D10 OPEN**; 18 of 22 ticks blocked by `data_guard`, including the one tick that carried a closed decision bar. No digest committed, so `daemon_reads_the_real_exchange` is **PENDING** |
| 5. Access control items 1 and 2 | **done, pushed** | `9a94026`, gate `p8-access-control` exit 0; criterion `console_refuses_a_foreign_origin` PASS |
| 6. F2 | **not started, on purpose** | conditional on 1–5 green; item 4 is not |
| **D11 Part 1** — the band names whose money the balance is | **BUILT, NOT PUSHED — awaiting a gate** | branch `phase-8-d11-part1-ungated` (`166965c`), restored into the working tree. 17 tests, `tests/console` 415 passed, 6/6 mutation arms killed, mypy and ruff clean |
| **D10 option (b)** — ruled, with three fixes | **not started** | next after F3's gate. Shared staleness key, stream heartbeat, freshness re-check before the order, record the fresh-pair fraction. Then a run of hours |

## What the operator has to decide before item 4 can close

D10 and D11 are WHAT decisions — they change what the system trades, what is written for every
tick, or what a number on screen means — so they were recorded and skipped rather than decided.
Full options and recommendations are in `decisions-2026-09-21.md`.

- **D10 — `data_guard` blocks the tick against the real universe.** It judges the whole tick on
  the oldest quote across all **668** subscribed USD pairs, and some illiquid pair is always more
  than 120 s stale: 18 of 22 ticks blocked, 0 tallies, 0 rejections, 0 orders.
- **D15 — not a defect, but it bounds D10's evidence.** The three unblocked ticks ended at
  engine 5 with PASS because no decision bar closed, which is by design. `decision_bar_s: 900`
  means the 21-minute run crossed **one** bar boundary, and that tick (cycle 19, 05:00:26Z) was
  the one `data_guard` blocked. **The D10 ruling wants a run of hours behind it**, not this one.
- **D11 — the screens show the paper ledger (5,000.00), not the real wallet.** By invariant 2's
  paper-ledger rule. A labelling fix is ~15 minutes; a real-balance panel belongs with the main
  window.
- **D12** (the Phase 0 seed names pairs `XBT/USD`) and **D14** (write hand-written files with LF)
  are carried forward, not blocking.

## Gates and pushes

Boundary gates run `gate.py` (`docs/build-log/phase-7/gate.py.txt`) in a clean worktree — reset
to HEAD, the boundary's files copied in, `verify.py --phase 8` there. `ACSOE_GATE_TREE` picks the
worktree: `ACSOE-gate`, `ACSOE-gate2`, `ACSOE-gate3`. **Two concurrent suites are fine; a third
was deliberately not run** while two were in flight, to keep a timing-sensitive test from going
red on contention. Logs: `logs/verify/gate-<label>.log`.

Pushed this session: `9b38a56` (Phase 8 files and costing) · `4316940` (docstring, task C0) ·
`1dab722` (C0's four criteria) · `17081e0` (`config/daemon.yaml`) · `a79c258` (the model ruling
and F2's cost) · `412ab7c` (fold 404 in `daemon.yaml`) · `829e4cc` (F1) · `9a94026` (access
control) · `6b39360`, `adeb13c`, `cc642d7`, `09acde1` (the paper trail and two corrections to it) ·
`cedddaf` (the `fees.py` read-only plumbing). A push, not a commit, is what makes work safe.

**Close the phase's work with a gate on the pushed tree**, not on copied working-tree files —
D14's reason. **But nothing can be gated until the crash clears.** `toolchain_green` is
registered for every phase, so the fault blocks every boundary, not just F3's. A chunked run
of the same 4,004 tests got **2,002 through in four processes** (`engines` 913, `research` 398,
`clients` 626, `core` 65) while `tests/platform` and the remainder crashed — so the machine can
work but cannot finish one long process, and **the crash is nowhere near F3**: the chunk holding
its tests passed 626 tests in 16 s. **First action in the morning: reboot,
then one gate run on the F3 boundary** (`gate_f3e.cmd` in the scratchpad, or the same four
files), and on green **merge `phase-8-f3-ungated` into `main`** — or commit the working tree,
which holds the identical blobs. Do not change the code to chase this.

## Do not, per the operator

Enable `mode: live`. Expose the UI beyond localhost. Create, rotate or revoke any API key.
Restart the recorder, the supervisor, `funding.py` or `fees.py` (code changes to `fees.py` are
fine; restarting it is the morning's job, with the new key). Retrain anything. Change any Phase 7
number, figure, document or database. Build beyond the six items.

## Evidence kept aside, not in the repository

In this session's scratchpad (`.../scratchpad/lead/`): `smoke-run2.sqlite` and its log (the clean
run), `smoke-run1-on-seed.sqlite` (the first attempt), `acsoe.sqlite.seeded-aside` and
`acsoe.sqlite.before-smoke`. **`data/db/acsoe.sqlite` was restored byte-identical** to its
pre-run state, verified with `cmp`.
