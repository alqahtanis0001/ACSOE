# Phase 7 handover, 2026-09-19 (lead session ending; written before launch)

**THE RUNS HAVE NOT BEEN LAUNCHED.**

## CURRENT STATE (keep this block current at every boundary; last updated 2026-09-19 ~18:30 local)

**Read this block first; it overrides anything older below.**

- **Lead:** a new session took over at about 18:25. The previous lead (`claude.exe` PID 1056) was
  found still alive and working, and **was closed by the operator**. None of its processes remain.
  Account: `lead.md`, "The previous lead was still running when its successor started".
- **Steps 3 and 5 are DONE and pushed:** the findings, including §7b, at `490f0d2`; the launcher's
  `--begin 2024-10-05T00:15:00` at `6f242e0`.
- **FINAL CODE: `19c5a11` (146 committed and pushed at 19:17).** Gate b13 green: 4 PASS, 0 FAIL,
  2 PENDING; 3925 passed. Nothing after it changes code. **The launch pins `-Commit 19c5a11…`.**
- **STOPPED FOR AN OPERATOR RULING (19:30).** The first gating rehearsal (`rehearsal-19c5a11`)
  **crashed natively in polars** (access violation) on tick 55. Two identical repros ran clean,
  so the crash is intermittent. Account: `lead.md`, "The gating rehearsal at `19c5a11` crashed
  natively in polars". **Running now, detached:** the rehearsal again (`rehearsal2-19c5a11`), and
  stress runs `repro\s1` (tier 3) and `repro\s2` (tier 5), all with faulthandler on. The dry run
  is DONE and inspected: `scout_inputs_unavailable` on tick 1 of every fresh run is expected
  (`lead.md`), and its databases are kept. **No launch** until the operator rules on crash
  recovery.
- **Step 4 (first attempt, crashed; see above): the gating rehearsal**, detached through WMI, in the
  git-clean main checkout. Output in `<new lead scratch>\lead\rehearsal-19c5a11\` (report.json,
  rehearsal.out.log). It PASSES only if
  `python check_rehearsal.py rehearsal <that dir>` prints ALL PASS; the script is
  `check_rehearsal.py.txt`. If it is still running after a connection loss, **leave it**. If it
  died, rerun it with the same command.
- **Step 6, overlapping it (operator-approved): the launcher's dry run** at `19c5a11`, into
  `data\db\phase7-dryrun-*.sqlite`. Keep those until both real runs are confirmed alive, then
  delete them.
- **Step 1 (140): DONE, `93e5ea1`, pushed.** Gate b12 green (4 PASS, 0 FAIL, 2 PENDING; 3890
  passed). The launch-precondition check (`check_rehearsal.py.txt`) was committed before the
  rehearsal at `3efe958`. **Step 2 (146):** `tests/verify` passed (494). Gate
  `b13-146-scout-tallies` is running in `ACSOE-gate2` on `3efe958` plus all 23 uncommitted files,
  which is the exact tree the 146 commit will make. Background, kept for the record:
  **the b11 gate is VOID**, because `engine.py` changed under it. It is re-gated
  as **`b12-140-shap-writer`**, on HEAD `b27d117` plus the exact 140 blobs c-eval kept in
  `<old scratchpad>/c-eval/pre146/`. They hash `c0a7fd5b`, `603ee59e` and `3b7d4601`, with
  `tests/engines/test_memory_shap.py` at `4fe77768`. The 140 commit stages those blobs, not the
  working tree's engine 19.
- **Step 2 (146):** **engine 19's half IS applied** in the main checkout (the list below said
  not). c-eval built and swept it: 8 of 8 mutants killed, all restored and checked. The three
  `DOCUMENTED_TABLES` edits, the `architecture-context.md` rows and a-replay's rehearsal check are
  on disk too. The working tree's engine 19 is 140+146, hashing `53e16db0` (`engine.py`),
  `dcde37cd` (`contracts.py`) and `5dd015bd` (`README.md`). **Next:** all of `tests/verify` on
  that state (running), then its own gate, commit and push.
- **Then:** step 4 (the gating rehearsal, run in the main checkout once it is git-clean at the
  final commit, with nothing edited while it runs), step 6 (the dry run) and step 7 (the launch).
  **The PID table below is filled in and pushed the moment both runs are alive.**
- **Scratch:** the new lead's is
  `C:\Users\saad2\AppData\Local\Temp\claude\C--Users-saad2-Documents-GitHub-ACSOE\de784c1c-85b7-4ce5-b14c-347c41714eba\scratchpad\lead\`
  (`gate.py` copied from `gate.py.txt`, identical). The old session's scratchpad is read-only
  from here on.
- **The recorder, supervisor, `funding.py`, `fees.py` and the manager** were alive at 18:20 and
  writing. Their supervisor tree was started at 16:01 local from `master.bat`, not at 05:08Z as
  section 6 says; they are alive, so nothing was done.
- **If you are a fresh session after a connection loss:** restart nothing that is running. Check
  the PID table, `git log origin/main`, and `logs/verify/gate-*.log` first.

**Read this first, then `feature-specs/PHASE-7-TASKS.md` (HANDOFF 2), then
`docs/build-log/phase-7/overnight-decisions-2026-09-19.md`.** The operator rules on the phase
close after seeing the simulation's output. Nobody marks Phase 7 green. The operator's report
deadline is **Sunday 2026-09-20 13:00**.

---

## 0. Standing instruction: THE WINDOW IS SHORTER, THE SYSTEM IS NOT

**The window is three months, locked by the operator (section 2).** Nothing else is cut. Every
engine runs and every gate runs: the full chain from guard through manage, exactly as it would run
live. No engine disabled, no gate skipped, no thinned tape, no sampled bars, no reduced pair
universe. **Do not read "three months" as licence to shorten anything else.**

## 1. State of the build

**HEAD on origin/main before this revision: `91d10e4`.** Commits made on 2026-09-19, oldest first:

| Commit | What |
|---|---|
| `5ed78d6` | the seven Phase 7 rulings applied to the specs; spec 75 resolved; the $60 checked; Phase 6 re-gated green (13/13) |
| `cd8131b` | spec 126: the rulings in the authority documents; the context half of 144; spec 145 written |
| `b2119fd` | `core/`: the `previous_now` seed for replay resume (D9) and the `context.mode` fix (F2); supervisor P3 |
| `4b32655` | 127 (recorded AssetPairs), 128 (weekly partitions), 130 (bucket table); recorder P1 (instrument channel) and P2 (fees.py) |
| `bd96346` | 137 (batched DI), 144's modelling half, 139's statistics and trial ledger (N = 1,679) |
| `df89e8a` | 136 (13-fold skeptic cap) and 135 (artefact assembly); the 26 `-p7` run directories are built in the gitignored `models/` |
| `ba5a1bb` | 144's engine 7 half; 139's engine 20 promotion gate; 140's leaderboard screen |
| `3d6ae12` | engine 5 13× faster, bit for bit (D16) |
| `04bc511` | 129, 131, 142, 132 (migration 0006, approvals, SHAP store), 133, 134, 138, 141, config |
| `ce43574` | `--log-dir`; the launcher first committed |
| `34a55d4` | **145**: every gate's verdict on both sides (`approvals.details`, `rejections.details`) |
| `9c42fba` | findings §7a: the declared substitutes and their limitations |
| `91d10e4` | the first version of this handover; the launcher (two runs), status and gate scripts |
| docs commits in between | handoffs, the four-to-two runs correction, open questions |

**Where every piece of uncommitted work sits** (the main checkout's working tree, unless noted):
- **140's SHAP writer (c-eval):** `src/acsoe/engines/memory/{engine.py,contracts.py,README.md}` and
  `tests/engines/test_memory_shap.py`. Built, 11 tests, 9/9 mutants killed; the full
  `tests/verify` run was pending.
- **146, B's migration 0007 (done, revised to the ruling: no `status`, `closed_bar_ts` from engine 3):**
  `db/migrations/0007_tick_tallies.sql`, `src/acsoe/clients/store/{contracts,client,migrations,__init__}.py`,
  `tests/db/test_migrations.py`, `tests/clients/store/test_scout_tallies.py`.
- **146, engine 19's half:** drafted in c-eval's scratchpad, `scratchpad/c-eval/draft146_patch.py`, in
  the session scratchpad
  `C:\Users\saad2\AppData\Local\Temp\claude\C--Users-saad2-Documents-GitHub-ACSOE\38924e42-53d2-4066-acd4-1a8a61a45926\scratchpad\`.
  **Not applied.** Apply it after 140 is committed.
- **C's `DOCUMENTED_TABLES` line for `scout_tallies`:** not yet written. It goes in `scripts/verify.py`,
  `tests/verify/conftest.py` and `tests/verify/test_phase0_criteria.py` (count 10 → 11), the same shape
  as the `approvals` line.
- **The lead's `architecture-context.md` rows for `scout_tallies`:** written, uncommitted.
- **a-replay's `scout_tallies` check:** `scripts/rehearse_replay_day.py` and
  `tests/scripts/test_replay_rehearsal_scripts.py`. It passes against the revised 0007; its final
  checks were running.

**Phase 7 criteria at `34a55d4`** (gate b10): 6 criteria, 4 PASS, 0 FAIL, 2 PENDING.
- PASS: `docs_vocabulary`, `toolchain_green` (3879 passed, 3 skipped), `promotion_gate_rejects_haircut_edge`,
  `fee_scenario_is_replay_only`.
- PENDING `research_screens_render`: waits on 140's SHAP writer, then 140's screens, after the run.
- PENDING `backtest_emits_alpha_report`: waits on the runs. Spec 143 commits each run's
  `tests/fixtures/phase7/run-digest-<name>.json`, the output of
  `python -m acsoe.research.attribution --db <run> --json <file>`.

**Gates** run in clean worktrees (`../ACSOE-gate`, `../ACSOE-gate2`) through `gate.py`
(`docs/build-log/phase-7/gate.py.txt`, copy it to scratch): `python gate.py <label> 7 <files or dirs…>`,
with `ACSOE_GATE_TREE=ACSOE-gate2` for the second worktree and `rel::alt` for a partial file.
**Before any DONE, every teammate runs all of `tests/verify`** (three gates went red on mutation
anchors today).

## 2. Rulings that are FIXED and must not be reopened

| Ruling | Why |
|---|---|
| **The window is THREE months, both tiers: folds 392–404, test weeks 2024-10-05 00:15Z to 2025-01-04 00:00Z.** Locked by the operator. | The report deadline is Sunday 13:00, and six months does not fit. |
| **Two runs: tiers 3 and 5, median spread column, expected-move ranking** | Tier 3 is reachable by a small account, tier 5 is the scale most never reach: the two ends of the cost-adaptive claim. |
| **No alphabetical pass, now or later, and no touching its code path or `config/default.yaml`** | Alphabetical is the name of the limitation the ranking removed, not a rival design. It answers "ranking against no ranking", not skill. No criterion requires it, and offline it gives 0–12 trades. The committed default is an open question for the operator (section 6). |
| **No q25/q75 spread runs** (spread sensitivity dropped from Phase 7) | Measured: more parallel runs slow the tier-3 run past the operator's limit. It is stated from the offline §3 comparison and a post-run repricing script, with its limit declared. |
| **N = 1,679, pre-registered, with its deliberate overcount of two** | Fixed before any figure exists. The ledger counts four Phase 7 runs and two run, which is the harsher direction. Never changed after a figure exists. |
| **HAC with an overlap-derived lag; Bonferroni as the headline; the unadjusted p-value, BH-FDR and the deflated Sharpe pre-registered alongside** | R10b's bar is fixed in code (`modelling/promotion.py`). The others are reported, never used to promote. |
| **R8's basket: the pairs held, while held, cash otherwise; its two secondary baskets computed after the run** (the held pairs held over the whole window, and the equal-weighted universe) | It isolates what the barriers and timing add. The secondaries are computable from stored data. |
| **F5's exit rule run as built** (a target exit sells at market on the next tick) | Changing it now would be changing the system to help it pass its own test. |
| **Invariant 10 stays: no labels on the live path** | Labelled outcomes are post-run arithmetic. |
| **Stop-at-first-block stays** | The engine contract. The record states which gates did not run. |
| **The declared substitutes, 2026 applied to 2024** (findings §7a) | Spread, depth, pair rules and fees, each with its source and date. |
| **The findings document is written and pushed BEFORE the launch** | The declared limitations must exist before any figure does. |
| **The gating rehearsal must SHOW SHAP files actually written** | Engine 19 treats a refused SHAP write as record-and-continue, so a silent zero is possible. |

## 3. Remaining steps, in order

1. **140's SHAP writer:** confirm c-eval's `tests/verify` run was clean, then gate it (boundary label
   e.g. `b11-140-shap-writer`), commit and push.
2. **146:** apply `draft146_patch.py` to engine 19 and add C's `DOCUMENTED_TABLES` line. Assemble
   the boundary: 0007, the store files and tests, engine 19 and its test, the three
   `DOCUMENTED_TABLES` files, the `architecture-context.md` rows, and a-replay's rehearsal-script
   check. **Everyone runs all of `tests/verify` first.** Gate, commit, push.
3. **The findings document**, committed and pushed **before the launch**:
   - the operator's section 4 wording (listed at the end of this file);
   - the four three-month limitations in section 5 below.
4. **The gating rehearsal** at the final commit:
   `python scripts/rehearse_replay_day.py --day 2024-10-20 --fixture tests/fixtures/phase7/rehearsal_2024-10-20 --out <dir> --tier 3 --fold 394`
   (about 1.6 h; the day is inside the three-month window). It must SHOW all of these, or stop:
   - `tree_unchanged` and `ranking_stop` false;
   - identical reruns, and every recomputation equal;
   - **SHAP files > 0**;
   - `approvals.details` and `rejections.details` filled;
   - `scout_tallies` with one row per tick on which engine 7 ran;
   - a non-null scenario digest.
5. **DONE (`6f242e0`): the launcher passes `--begin 2024-10-05T00:15:00`, and its dry run ticks inside the window.** As it stood before: It passes no `--begin` today, so as committed it
   would run six months. Add `--begin 2024-10-05T00:15:00` to both real command lines. Change the
   dry run's `--until 2024-07-06T00:15:00` to `--begin 2024-10-05T00:15:00 --until 2024-10-05T00:15:00`.
   Commit it.
6. **The launcher's dry run** (one tick per run, into throwaway databases), then delete those
   databases.
7. **Launch**, then fill in the PID table below and push.

**The launcher has been rewritten since it last ran, and its two-run form was executed once, as a dry
run at `9c42fba`.** Its path is `docs/build-log/phase-7/launch_phase7.ps1.txt`; copy it to
`launch_phase7.ps1`. Invoke it as `powershell -NoProfile -File launch_phase7.ps1 -Commit <final sha> [-DryRun]`.
It creates `C:\Users\saad2\Documents\GitHub\ACSOE-run-<sha7>`, with NTFS junctions
`data` → `C:\Users\saad2\Documents\GitHub\ACSOE\data` and `models` → `…\ACSOE\models`. It checks that
`acsoe` imports from the worktree, starts each run through WMI `Win32_Process.Create`, and writes
`<worktree>\phase7-runs\launch.json` with the cmd and python PIDs and the parent process name.

**The exact command per run, after step 5** (cwd = the worktree, `PYTHONPATH=<worktree>\src`,
`PYTHONDONTWRITEBYTECODE=1`, interpreter `C:\Users\saad2\Documents\GitHub\ACSOE\.venv\Scripts\python.exe`):
```
python -m acsoe.cli.main --config config/default.yaml research backtest --db data\db\phase7-t3-expected_move.sqlite --ranking expected_move --fee-tier 3 --begin 2024-10-05T00:15:00 --log-dir <worktree>\phase7-runs\logs-t3-expected_move
python -m acsoe.cli.main --config config/default.yaml research backtest --db data\db\phase7-t5-expected_move.sqlite --ranking expected_move --fee-tier 5 --begin 2024-10-05T00:15:00 --log-dir <worktree>\phase7-runs\logs-t5-expected_move
```
With no `--until`, a run ends at fold 404's test close, 2025-01-04 00:00Z.

**PID table, to be filled in at launch and pushed:**

| Run | cmd PID | python PID | DB | Run record | Launched |
|---|---|---|---|---|---|
| t3-expected_move | | | `C:\Users\saad2\Documents\GitHub\ACSOE\data\db\phase7-t3-expected_move.sqlite` | same path + `.runrecord.jsonl` | |
| t5-expected_move | | | `C:\Users\saad2\Documents\GitHub\ACSOE\data\db\phase7-t5-expected_move.sqlite` | same path + `.runrecord.jsonl` | |

**Status, one line** (`docs/build-log/phase-7/phase7_status.ps1.txt`, copied to `phase7_status.ps1`):
`powershell -NoProfile -File phase7_status.ps1 C:\Users\saad2\Documents\GitHub\ACSOE-run-<sha7>\phase7-runs\launch.json`.
It prints, per run, whether the python PID is alive and the run record's age in seconds. It was
tested against the dry run.

**Detachment, as it actually stands:**
- Each run's `cmd.exe` has parent `WmiPrvSE.exe` (the WMI provider host), outside this session and
  outside its process tree. This session's own python is in no job object.
- Each run sits inside the WMI provider host's job object. **Whether a run survives that host's job
  being closed was never proven.** Killing the host was refused (access denied). **Do not attempt to
  prove it by killing anything.**
- Phase 5's 24-hour training run was launched the same way and survived independently of its session.

**If a run dies:** re-run its exact command with `--resume` added, from the same worktree, with the
same env, through WMI. It continues after the later of the store's last tick and the run record,
with `previous_now` seeded. Cost: the in-flight tick, plus about 15–20 s to reload, plus the time
until someone notices.

## 4. Measured timing inputs (do not re-derive)

- **Bar tick: 5.00 s** at two runs in parallel (4.76 s alone, 6.00 s at four, 6.87 s at six; 12 bars
  of 2024-10-20, frozen commit `34a55d4`).
- **Minute tick: about 4.3 s** at two in parallel (4.1 s measured alone, × 1.05).
- **82 exposed minutes per approval**, from the rehearsal day (328 minute ticks for 4 approvals; the
  window's busiest day).
- **Approvals = expected trades ÷ 0.75** (3 of 4 rehearsal entries filled).
- **8,736 bar ticks over three months** (91 days × 96).
- **The machine has 32 logical cores, not 24.** One run keeps 1.6–2 cores busy, and the machine is
  never saturated (43% busy at six runs). The slowdown at higher parallelism is contention, not a
  core shortage.

**Expected wall-clock, three months, two in parallel, from a launch around 21:30 on 2026-09-19:**

| | Bar ticks | Minute ticks | Total | Finish |
|---|---|---|---|---|
| Tier 3 | 8,736 × 5.00 s = 12.1 h | 11–15 approvals × 82 min × 4.3 s = 1.1–1.5 h | **≈13.2–13.6 h** | ≈Sunday 11:00 |
| Tier 5 | 12.1 h | 37–73 approvals × 82 min × 4.3 s = 3.6–7.2 h | **≈15.7–19.3 h** | ≈Sunday 13:00–17:00 |

**Tier 5 is the softer number:** if the true average hold is about 4 h rather than 82 minutes, it is
22–32 h. The earlier **45–60 h figure (six months) was a guess at multi-hour holds, not a
measurement.** At the rehearsal's measured 82 minutes per approval, six-month tier 5 is **31–41 h**.
Do not carry the old number.

## 5. Four limitations to write into the findings BEFORE launch (chosen constraints, not findings)

1. **Three months and 13 retrained models, not six and 26.** The weekly-retraining claim rests on 13
   retrains and one season.
2. **The window sits inside a strong directional market.** That narrows §6a's existing warning.
   Attribution has 91 daily returns.
3. **Expected trades are about 8–11 at tier 3 and 27–55 at tier 5.** Tier 3 is at or below spec 139's
   10-trade threshold, so the promotion gate may report no interval, and tier 3's result is
   descriptive.
4. **The three-month cell was never measured offline.** The trade estimates are scaled from the six-
   and twelve-month cells (§4).

## 6. What the next session must NOT do

- Stop, restart or reconfigure a running run.
- Touch the frozen run worktree or its junctions until both runs end.
- Touch the recorder, the supervisor, `funding.py` or `fees.py` (running since 05:08:44Z, from the
  `master.bat` console). A dead one is restarted, never a live one.
- Retrain anything.
- Change any statistic, threshold or rule in section 2 after a figure exists.
- Shorten anything but the window (section 0).

## 7. Post-run work (start only after both runs are live)

- Engine 20 statistics: the unadjusted p-value, the overlap count behind the HAC lag, BH-FDR, and the
  deflated Sharpe (already reported).
- The labelled outcome and labelled return per closed trade, plus bars between signal and fill (from
  `approvals.ts`, `placed_at`, `opened_at` and the committed partitions via `research/labelling`).
- The two secondary baskets.
- Q18: which pair was excluded for which reason, recomputed offline from the fixtures, the stored
  equity, cash and positions, and engine 7's own filter. It self-checks against `scout_tallies`'
  counts.
- Spread repricing: re-price the trades that happened under another spread. **It cannot say which
  trades a different spread would have produced.**
- A watchdog: PID alive and run record mtime under 10 minutes, writing an alert file. A scheduled
  task, **not** integrated into the recorder's supervisor.
- After the runs: `research.attribution --json` per run into `tests/fixtures/phase7/`; findings
  substitution; 140's screens.

## 8. Open questions carried forward, each deliberately left

- **The committed daemon still ranks alphabetically** (`scout.rank_feature` is absent from
  `config/default.yaml`). Changing it changes the daemon: the operator's ruling. The YAML's own
  comment is stale.
- **Prerequisites 2–4** (training memory hazards): nothing retrains in Phase 7.
- **F1 and F3, the live-path defects:** no `recent_trades` on the live client; `TradeVolume` mapped
  from the invented shape with no pair; `AssetPairs` keyed by REST names; engine 7 ignores pair
  status. Each changes live behaviour. Phase 8.
- **The flat-pair tie-break spelling** (grid `EURUSD` against engine 7's `BTC/USD`): only tied flat
  pairs, and no trade can differ.
- **Stale code comments and labels** (`cli/research.py:548–549, 573`, `replay_scenario.py:552`,
  `trial_ledger.py:68`, two test comments): changing them needs gates, and they change no behaviour.
- **A separate read-only key for `fees.py`:** the operator's action. Until then, nonces can collide
  once the daemon runs.

## 9. What went wrong today, and what was learned

- **The build estimate ran about 5 h over.** The 12–17 h figure was a sum of parts, not a measurement,
  and the run cost was wrong by 3× until it was measured. Say "estimate" and measure before
  promising a time.
- **A partial edit failed on CRLF.** A multi-file edit wrote two files and failed on a third that was
  CRLF. A blind re-run would have duplicated two appended entries. Caught by counting the entries and
  re-running only the failed part; a grep afterwards found one missed line. Normalise line endings
  first, and grep for the old wording after every multi-file edit.
- **Heredocs:** backticks in a heredoc ran as commands and deleted words; another heredoc failed to
  parse. Write scripts with the file tool.
- **Red gates on mutation anchors (b4, b5, b6):** new code copying an existing idiom breaks
  criterion-test anchors. Run all of `tests/verify` before any DONE.
- **A gate that never ran reported success** (argument overflow, exit 126). Confirm a launch by its
  evidence, never by the return code.

## UPDATE: the findings are WRITTEN (`490f0d2`, pushed before launch)

§R.6, §6a and §7a are set to three months. **§7b is new, written before any figure:** the four
three-month limitations; the promotion bar recomputed at three-month trade counts (tier 3 needs
4.9–5.5% per trade and cannot clear it; tier 5 needs 1.35–2.14% and cannot plausibly clear it, so
non-promotion is the expected outcome of the design); N = 1,679 with its overcount; the benchmarks
with no claimed citation; F4 as "no detectable difference"; F6 split into measured against
calculated; and F5. **Step 3 of section 3 is done.** The list below is how it stood before, kept for
the record.

## Is `docs/dataset/phase-7-findings.md` fully written?

- **Operator section 13 (the declared substitutes): WRITTEN**, as §7a (`9c42fba`).
- **Operator section 4 (the wording corrections): NOT WRITTEN.** Missing:
  - F4 as "no detectable difference", with its n and noise band;
  - F6 split into measured (c-models over 2,216,920 rows) against calculated (the lowest bar of about
    1.30%);
  - R8's no-claimed-citation line;
  - R10b's trial-count statement (N = 1,679 with its overcount of two);
  - the promotion bar's expected unreachability at these trade counts, re-stated for three months
    (tier 3 below 10 trades, no interval).
- **The four three-month limitations (section 5): NOT WRITTEN.**
- **All of it must be committed and pushed BEFORE the launch.**
