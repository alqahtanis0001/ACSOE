% Captions for the Phase 7 figures. Generated by make_phase7_figures.py.
\caption{Account equity through the simulated window, both fee tiers, from 5,000 USD. Markers show each trade's close; the dotted verticals mark the protective freeze on each run, labelled with its date and trigger. Replay of 2024-10-05 to 2025-01-04, folds 392-404.}
%% file: F1_equity_curves.pdf / F1_equity_curves.png / F1_equity_curves.csv

\caption{Tier 3's drawdown from its stored peak equity, against the 10\% limit (\texttt{safety.max\_drawdown\_pct}). The LUNA/USD stop of 9 December, which realised -6.12\% on a one-minute hold, is marked; the limit is crossed on 19 December and the run freezes.}
%% file: F2_tier3_drawdown.pdf / F2_tier3_drawdown.png / F2_tier3_drawdown.csv

\caption{Realised profit and loss per closed trade, net of fees, in time order. Hatched bars are stop exits, plain bars target exits; lighter edges are tier 5. The LUNA/USD stop is 2.5 times the size of any other loss.}
%% file: F3_per_trade_pnl.pdf / F3_per_trade_pnl.png / F3_per_trade_pnl.csv

\caption{Realised return at exit against the barrier the trade was aiming at, per closed trade, both tiers, gross of fees. Points on the 45-degree line realised exactly their barrier. Every stop lies below the line; the exit rule caps gains at the target but does not cap losses at the stop.}
%% file: F4_barrier_vs_realised.pdf / F4_barrier_vs_realised.png / F4_barrier_vs_realised.csv

\caption{The decision funnel for both runs, log scale: bar ticks run, ticks where engine 7 published a candidate, candidates refused, approvals, entry orders, fills and closed trades. Every refusal in both runs came from engine 10 \texttt{cost}.}
%% file: F5_decision_funnel.pdf / F5_decision_funnel.png / F5_decision_funnel.csv

\caption{Candidates refused per simulated week, by refusing gate and reason. Gaps are weeks in which the run was frozen and examined no candidate.}
%% file: F6_rejections_over_time.pdf / F6_rejections_over_time.png / F6_rejections_over_time.csv

\caption{When each run could trade. Horizontal bars are open positions; the shaded region is the frozen remainder of the window after the protective breaker fired.}
%% file: F7_trading_vs_frozen.pdf / F7_trading_vs_frozen.png / F7_trading_vs_frozen.csv

\caption{The engine chain as it ran: guard, opportunity and manage, with the gates marked, the point at which a refusal stops the chain, and what engine 19 records on every tick.}
%% file: F8_engine_chain.pdf / F8_engine_chain.png / F8_engine_chain.csv

